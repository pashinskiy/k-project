import React from 'react'
import CardWidget from './cardWidget'

export default function CategoriesWidgetSinglePanel(categories){
    const data = categories.map(category => {
        
    })
    return(
        <CardWidget />

    )
}